<UserControl x:Class="Client.View.UserControls.MessagesViews.MessageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Client.View.UserControls.MessagesViews"
             mc:Ignorable="d">
    <UserControl.Resources>
        <Style x:Key="StyleTextBox" TargetType="TextBox">
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <TextBox Grid.Row="0" Text="Ваши заявки - Наша реклама" 
                 TextWrapping="Wrap" AcceptsReturn="True" HorizontalAlignment="Left" VerticalAlignment="Top" 
                 Style="{StaticResource StyleTextBox}"/>
        <TextBox Grid.Row="1" Text="10:01 26.01.2020" HorizontalAlignment="Right" VerticalAlignment="Bottom"
                 Style="{StaticResource StyleTextBox}"/>
    </Grid>
</UserControl>
