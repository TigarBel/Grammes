<UserControl x:Class="Client.View.UserControls.MessagesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:messagesViews="clr-namespace:Client.View.UserControls.MessagesViews"
             mc:Ignorable="d">
    <UserControl.Resources>
        <Style x:Key="MyWaterMarkStyle" TargetType="{x:Type TextBox}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBox}">
                        <Grid>
                            <Border Background="White" BorderBrush="#FF7D8683" BorderThickness="1"/>
                            <ScrollViewer x:Name="PART_ContentHost" Margin="5,0,0,0" VerticalAlignment="Center" />
                            <Label Margin="5,0,0,0" x:Name="WaterMarkLabel" Content="{TemplateBinding Tag}" 
                                   VerticalAlignment="Center" FontStyle="Italic"
                                   Visibility="Collapsed" Foreground="Gray" FontFamily="Arial"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="Text" Value=""/>
                                </MultiTrigger.Conditions>
                                <Setter Property="Visibility" TargetName="WaterMarkLabel" Value="Visible"/>
                            </MultiTrigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="LightGray"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="3*" MinHeight="100"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*" MinHeight="30"/>
        </Grid.RowDefinitions>

        <Label Content="Chat name: ???" Grid.Row="0" HorizontalAlignment="Left" VerticalAlignment="Top"/>

        <ListBox Grid.Row="1" HorizontalContentAlignment="Stretch">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>
            </Grid>
            <messagesViews:MessageView Grid.Column="1" HorizontalAlignment="Right"/>
            <messagesViews:MessageView Grid.Column="0" HorizontalAlignment="Left"/>
            <messagesViews:MessageView Grid.Column="1" HorizontalAlignment="Right"/>
            <messagesViews:MessageView Grid.Column="1" HorizontalAlignment="Right"/>
            <messagesViews:MessageView Grid.Column="0" HorizontalAlignment="Left"/>
            <messagesViews:MessageView Grid.Column="1" HorizontalAlignment="Right"/>
            <messagesViews:MessageView Grid.Column="1" HorizontalAlignment="Right"/>
            <messagesViews:MessageView Grid.Column="0" HorizontalAlignment="Left"/>
            <messagesViews:MessageView Grid.Column="1" HorizontalAlignment="Right"/>
            <messagesViews:MessageView Grid.Column="1" HorizontalAlignment="Right"/>
            <messagesViews:MessageView Grid.Column="0" HorizontalAlignment="Left"/>
        </ListBox>

        <GridSplitter Grid.Row="2" HorizontalAlignment="Stretch" Height="5"/>

        <TextBox Grid.Row="3" TextWrapping="Wrap" AcceptsReturn="True" FontSize="13"
                 Style="{StaticResource MyWaterMarkStyle}" Tag="Enter message..."/>

        <Button Grid.Row="3" HorizontalAlignment="Right" VerticalAlignment="Bottom" Content="Send"
                Background="Transparent" Margin="3" BorderThickness="0" FontSize="15"
                IsEnabled="True" />
    </Grid>
</UserControl>
